## 1. Dependencies

- [x] 1.1 Install `react`, `react-dom`, `@types/react`, `@types/react-dom` via pnpm
- [x] 1.2 Enable JSX in `tsconfig.json` by setting `"jsx": "react-jsx"`

## 2. Layout Components

- [x] 2.1 Create `src/app-components/Header.tsx` with placeholder text "Header"
- [x] 2.2 Create `src/app-components/Navigation.tsx` with placeholder text "Navigation"
- [x] 2.3 Create `src/app-components/MainBody.tsx` with placeholder text "MainBody"
- [x] 2.4 Create `src/app-components/Footer.tsx` with placeholder text "Footer"

## 3. App Root

- [x] 3.1 Create `src/App.tsx` that imports and renders Header, Navigation, MainBody, and Footer in order
- [x] 3.2 Update `src/index.ts` to import and reference `App` (or add a note that a bundler/dev server is needed to render in browser)

## 4. Verification

- [x] 4.1 Run `pnpm type-check` to confirm TypeScript compiles with no errors
